import 'package:flutter/material.dart';

BoxDecoration iconDecor = BoxDecoration(
  shape: BoxShape.rectangle,
  color: Colors.black54,
  borderRadius: const BorderRadius.all(Radius.circular(14)),
  border: Border.all(
    color: Colors.transparent,
    width: 0.75,
  ),
);

BoxDecoration boxDecor = BoxDecoration(
  color: Colors.black,
  shape: BoxShape.rectangle,
  borderRadius: const BorderRadius.all(Radius.circular(12)),
  border: Border.all(
    color: const Color.fromARGB(255, 153, 198, 218),
    width: 0.75,
  ),
);

BoxDecoration numDecor = BoxDecoration(
  color: Colors.black,
  shape: BoxShape.rectangle,
  borderRadius: const BorderRadius.all(Radius.circular(12)),
  border: Border.all(
    color: Colors.black,
    width: 1.0,
  ),
);
