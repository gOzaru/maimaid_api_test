// ignore_for_file: constant_identifier_names
enum WgDialogType { SUCCESS, MESSAGE, WARNING, ERROR }

enum DataType { PUBLISHER, ALBUM, SONG }

enum SongType { EPIC, HAPPY, LULLABY, MIX, NATURE, RELAX }

enum SongStatus { DENIED, NEW, ACCEPTED, NONE, DISABLED, ALL, LIST}

enum IAPsType { MUSIC, THERAPY, MUSICTHERAPY, GOAL, DIARY, HELP, EDUCATION }

enum RadioType { STRING, INT, DOUBLE }

enum FieldType { TYPEINCOME, TYPELICENSE, DURATION }

enum StatusType { EMPTY, UPLOADING, CHECKED, COMPLETED, READY, ERROR }

enum EmailType { NEW, REPLACE, DISABLED }

enum CastType { STRING, INT, DOUBLE }

enum ImageType { BACKGROUND, LOGO }

enum TextType { SHORT, LONG }

enum IconType { EDIT, ADD, DELETE, NONE }

enum FilesType { URL, BYTES }

enum PubType { REGISTERED, UNREGISTERED }
